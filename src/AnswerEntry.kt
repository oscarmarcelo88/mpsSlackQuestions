package com.example2

data class AnswerEntry(val text: String, val timestamp: String)

val answerEntries = mutableListOf(
    AnswerEntry(
        "The drive to develop!",
        "...it's what keeps me going."

    )
)